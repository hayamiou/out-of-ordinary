<h1>Your dashboard :</h1>

<div class="card-booking">
  <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
  <div class="card-booking-infos">
    <h1>Your dashboard :</h1>
  </div>
</div>


<div class="row">
  <% @my_bookings.each do |my_booking| %>
    <div class="col-12 col-sm-6 col-md-4 col-lg-4">
      <div class="card-dashboard">
        <div class="picture">
          <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg">
        </div>
        <div class="card-dashboard-infos">
          <h2 class="text-truncate p-2"><%= my_booking.offer.title %></h2>
          <p><strong><%= my_booking.total_prices %></strong> € </p>
          <p>From <%= my_booking.date_start %> to <%= my_booking.date_end%></p>
          <p>For <%= my_booking.number_of_persons %> 👫👬👭</p>
          <% if my_booking.accepted %>
            <button class="btn btn-success" disabled="true">:coche_trait_plein: Accepted</button>
          <% elsif my_booking.accepted == false %>
            <button class="btn btn-danger" disabled="true">:signe_de_multiplication_trait_plein: Declined</button>
          <% else my_booking.accepted = nil %>
            <%= link_to "Accept", dashboards_my_bookings_path(my_booking, accepted: true), data: { turbo_method: :patch }, class:"btn btn-success" %>
            <%= link_to "Decline", dashboards_my_bookings_path(my_booking, accepted: false), data: { turbo_method: :patch }, class:"btn btn-danger" %>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>
